#include "config.h"

// Пустой файл.
// Раньше здесь была переменная `NTP_SERVER`, но в `config.h` она задана как макрос
// `#define NTP_SERVER "pool.ntp.org"`, из-за чего получался конфликт имён при компиляции.
